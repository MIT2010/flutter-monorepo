# Register bricks which can be consumed via the Mason CLI (`mason get`,
# then `mason make <brick>`). See bricks/feature/README.md for usage and
# ADR-009 in ARCHITECTURE.md for what the feature brick deliberately does
# and does not generate.
bricks:
  feature:
    path: bricks/feature
