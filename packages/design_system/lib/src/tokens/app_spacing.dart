/// Spacing scale used everywhere instead of ad-hoc `SizedBox`/`EdgeInsets`
/// values, so density stays consistent across every screen (§16).
class AppSpacing {
  const AppSpacing._();

  static const double xs = 4.0;
  static const double sm = 8.0;
  static const double md = 16.0;
  static const double lg = 24.0;
  static const double xl = 32.0;
}
